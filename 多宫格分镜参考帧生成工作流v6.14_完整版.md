# 多宫格分镜参考帧生成工作流（v6.14 完整版）

## 0. 版本定位

v6.14 是在 v6.8–v6.13 基础上的完整整合版，目标是为 **多宫格参考帧生成** 提供一套从资源库构建、基础帧生成、再编辑阶段镜头重设计，到最终九宫格筛选的完整工作流。

v6.14 的核心修正主要集中在两个方面：

1. **基础帧阶段**继续坚持资源依赖调度。新人物、新道具、核心商品不能只靠文字生成，必须通过资源库和 Reference Resolver 显式调度；基础帧负责锁定剧情骨架、空间关系和实体一致性。
2. **再编辑阶段**进一步稳定为 **Story-State / Screen-Content + Camera Transformation 双段式表层 Prompt**：
   - 第一段负责交代这帧画面本身正在呈现什么；
   - 第二段负责交代镜头类型，以及镜头要强调的细节；
   - 不再使用 `Keep ...`、`Preserve ...` 这类显式保持句；
   - 不再把内部审查项、资源约束清单、长串禁止项直接搬进最终表层 prompt。

v6.14 总结为一句话：

```text
Base frames lock story continuity.
Edit frames redesign the camera.
Resource selection must be dependency-aware.
Edit prompts must use a Story-State block plus a Camera block.
```

中文概括：

```text
基础帧负责剧情连续性；编辑帧负责镜头再设计；参考图调度必须依赖资源库；再编辑 prompt 必须采用“画面内容 + 镜头描述”的双段式结构。
```

---

## 1. 总体流程

```text
Step 1   接收用户 idea、风格、帧数 N、特殊要求
Step 2   构建资源库：人物、场景、道具、风格参考图
Step 3   扫描资源库并解析 Resource Cards
Step 4   做资源审查：可继承身份、可用道具状态、真实场景锚点、不可发明元素
Step 5   规划 N 张 base frames，并建立 Frame Entity Plan
Step 6   对每个 base frame 运行 Reference Resolver
Step 7   逐帧生成 case_base_frame01.png ~ case_base_frameNN.png
Step 8   对 base frames 做剧情与资源一致性质检
Step 9   输出 edit-stage 镜头重设计规划表
Step 10  用 Edit Prompt Compiler 为每帧编译 v6.14 再编辑 prompt
Step 11  逐帧生成 case_edit_frame01.png ~ case_edit_frameNN.png
Step 12  做 camera delta / story readability / consistency 筛选
Step 13  输出最终九宫格或最终参考帧集
```

---

## 2. 帧数规则

- 用户未指定帧数：默认 `N = 9`
- 用户明确指定帧数：严格按用户指定执行
- 用户希望“短一点”：建议 `N = 5` 或 `N = 6`
- `N = 9` 时使用标准九宫格逻辑
- `N ≠ 9` 时由标准 9 beat 骨架压缩或扩展

### 2.1 标准 9 beat

| Beat | 功能 |
|---|---|
| B1 | 开场：人物与场景关系 |
| B2 | 新人物进入 / 行动位置建立 |
| B3 | 关键道具出现 / 动作开始 |
| B4 | 接近目标 / 动作发起 |
| B5 | 首次变化 / 冲突启动 |
| B6 | 变化升级 / 反应增强 |
| B7 | 结果显现 |
| B8 | 处理结果 / 互动升级 |
| B9 | 收束 / 余波 / 记忆点 |

### 2.2 压缩参考

- `N = 5`：B1 / B2+B3 / B4+B5 / B6+B7 / B8+B9
- `N = 6`：B1 / B2 / B3+B4 / B5+B6 / B7 / B8+B9

---

## 3. 资源库扫描与 Resource Cards

### 3.1 资源来源

资源可以来自：

1. 用户直接上传；
2. 从零生成；
3. 从已有帧中裁剪、清洗或编辑得到。

无论来源如何，都必须进入资源库，并被解析为 Resource Card。

### 3.2 推荐命名

```text
case_char_01.png
case_char_01_clean.png
case_char_01_with_prop_01.png
case_char_02.png
case_scene_01.png
case_prop_01.png
case_style_01.png
case_base_frame01.png
case_edit_frame01.png
```

### 3.3 Resource Card 字段

```json
{
  "file": "case_prop_01.png",
  "resource_type": "prop",
  "entity_id": "universe_ring",
  "display_name": "golden Universe Ring",
  "identity_sensitive": true,
  "hero_product": true,
  "visual_traits": [
    "golden circular ring",
    "warm orange glow",
    "engraved cloud patterns"
  ],
  "usable_for": [
    "first appearance",
    "action shot",
    "close-up shot"
  ],
  "must_reference_when_first_appears": true,
  "must_reference_when_visible": true
}
```

### 3.4 组合资源图

当人物与核心道具需要高频稳定组合出现时，应允许使用组合资源图：

```text
case_char_01_clean.png
case_char_01_with_prop_01.png
case_prop_01.png
```

使用原则：
- 不拿道具的帧，用 `clean`
- 首次拿道具的帧，用 `with_prop` 或标准 prop 图
- 如果参考图预算有限，优先考虑组合资源图

---

## 4. 资源审查规则

在写任何 prompt 之前，必须先明确：

1. 每个角色资源图能稳定继承哪些身份信息；
2. 是否有 clean / with-prop 两种状态；
3. 场景图中真实存在什么锚点；
4. 道具图的关键识别点是什么；
5. 哪些内容不能从文本临时发明；
6. 哪些道具属于 hero product，必须持续约束。

核心原则：

```text
Storyboard planning must follow selected resource images.
```

---

## 5. Frame Entity Plan

每帧在生成前都要先定义实体状态。

每帧至少标注：
- 当前可见人物
- 当前可见道具
- 当前 Scene Core
- 是否有首次出现的人物 / 道具
- 是否有核心商品可见
- 当前视觉焦点
- 当前帧禁止出现的实体

示例：

| Frame | Story State | Visible Characters | Visible Props | New Entities | Hero Product Visible | Focus | Forbidden |
|---|---|---|---|---|---|---|---|
| frame01 | 哪吒躺在火焰莲花中央 | Nezha | none | Nezha | no | Nezha + lotus | Universe Ring |
| frame02 | 乾坤圈靠近 | Nezha | Universe Ring | Universe Ring | yes | ring + Nezha | seated pose |
| frame03 | 手指触碰乾坤圈 | Nezha | Universe Ring | none | yes | hand + ring | ring on wrist |

---

## 6. Reference Resolver

### 6.1 核心目标

不要默认：

```text
current frame = previous frame only
```

而要默认：

```text
current frame = previous frame + required resource references for current visible entities
```

### 6.2 首现实体规则

如果当前帧出现了上一帧没有的人物、道具、商品，则该实体的标准资源图必须被显式引用。

### 6.3 Hero Product 持续约束规则

只要核心商品清晰可见，尤其是产品展示帧、动作帧、反应帧、结果帧，都必须由标准 prop 参考图或已验证稳定的上一帧持续约束。

### 6.4 参考图优先级

```text
1. previous verified frame
2. newly appearing character / prop
3. hero product reference
4. focus character reference
5. scene reference
6. style reference
```

### 6.5 输出格式示例

```json
{
  "frame": "case_base_frame03.png",
  "story_state": "Nezha touches the floating Universe Ring",
  "visible_entities": ["Nezha", "Universe Ring"],
  "new_entities": [],
  "hero_product_visible": true,
  "focus_entities": ["Nezha", "Universe Ring"],
  "reference_images": [
    "case_base_frame02.png",
    "case_prop_01.png"
  ]
}
```

---

## 7. Scene Core / Character / Camera Topology

每帧先确定 Scene Core。

典型 Scene Core：
- 开场：场景与主体位置关系
- 新道具出现：人物手部与道具关系
- 动作帧：动作轨迹、施力方向、目标关系
- 反应帧：表情、身体反应、结果
- 收束帧：主体姿态、余波、产品记忆点

人物与 Scene Core 拓扑规则：
- front / three-quarter front：Camera → Scene Core → Character
- back / three-quarter back：Camera → Character → Scene Core
- side / three-quarter side：Character beside Scene Core

---

## 8. Anchor Visibility Budget

关键锚点必须可见。

如果 Scene Core 是人物与道具：
至少应看到：
- 人物
- 道具整体轮廓
- 人物与道具的动作关系
- 主要场景锚点之一
- 基础光源方向

如果 Scene Core 是产品：
至少应看到：
- 产品整体
- 关键识别结构
- 颜色分布
- 与人物的关系
- 产品朝向 / 作用方向

---

## 9. 第一阶段：Base Frame 规则

基础帧只负责锁定剧情骨架，不追求复杂镜头。

### 9.1 Base Frame 镜头范围

只使用：
- `wide shot`
- `medium shot`

禁止：
- close-up
- insert shot
- overhead
- extreme low-angle
- 复杂镜头旋转

### 9.2 Base Frame Prompt 必须包含

1. 真实参考图文件名
2. 一个确定的基础镜头
3. 当前 Scene Core
4. 人物与 Scene Core 的拓扑关系
5. Subject–Object Relation
6. Subject–Camera Relation
7. 关键锚点可见性
8. 必须保持的人物 / 道具身份
9. 禁止项

### 9.3 Base Frame Prompt 模板

```text
Generate case_base_frame0x.png: Use [previous frame] as the source for the exact story continuity, scene layout, character positions, object positions, and lighting direction. Use [required resource image] as the strict reference for [new or hero entity], preserving [key identity traits]. Camera setup: [wide shot / medium shot] with [subject-camera relation], clearly showing [Scene Core and anchor details]. Place [subject] [spatial relation to Scene Core], while keeping [continuity elements] unchanged. Do not [forbidden entity leakage], do not [identity drift], and do not [scene invention]. [Target style].
```

---

## 10. Base Frame 候选选择优先级

1. Reference Dependency Correctness
2. New Entity Consistency
3. Hero Product Consistency
4. Story State Accuracy
5. Scene Composition Accuracy
6. Subject–Object Relation
7. Subject–Camera Relation
8. Anchor Visibility
9. Visual Quality

### 10.1 硬性否决

- 新人物不像人物参考图：淘汰
- 核心商品形状、轮廓、关键特征不一致：淘汰
- 使用了场景参考图中不存在的锚点：淘汰
- Scene Core 被挡住：淘汰

---

## 11. 第二阶段：Edit Frame 总原则

第二阶段不是轻微再编辑，而是：

```text
Camera redesign based on the base frame.
```

主目标：
- 景别变化
- 机位变化
- 焦点变化
- 前后景变化
- 构图变化
- 视觉叙事重点变化

附带目标才是：
- 补光
- 修手
- 修衣摆
- 清背景
- 统一色调

---

## 12. Edit 阶段参考图策略（Base-only）

### 12.1 默认规则

再编辑阶段默认只使用对应 base frame 作为实际图像参考：

```text
actual_reference_images = [case_base_frame0x.png]
```

人物资源图、道具资源图仍然用于：
- 分镜规划
- 内部一致性审查
- 候选筛选
- 异常纠偏

但不默认再输入模型。

### 12.2 只有以下情况才允许额外加入人物 / 道具资源图

1. base frame 中人物身份已经明显错误；
2. base frame 中道具形状已经明显错误；
3. 本轮目标是纠偏，而不是镜头再设计；
4. 用户明确要求用某张资源图重新校正。

---

## 13. Camera Delta Requirement

每张 `case_edit_frame0x.png` 相比对应 `case_base_frame0x.png`，必须有明确镜头变化。

至少满足以下一项：

1. Shot scale delta
2. Camera angle delta
3. Camera side delta
4. Composition delta
5. Focus delta
6. Depth delta

如果 edited frame 只是补光、锐化、清背景，而没有真正 camera delta，则判为失败。

---

## 14. 镜头多样性预算

### 14.1 N = 9

至少 6 帧有明确 camera delta，推荐覆盖：
- 2–3 帧 wide / stronger wide
- 2–3 帧 medium / medium close-up
- 1–2 帧 close-up / insert
- 1 帧 overhead / high-angle
- 1 帧 low-angle / OTS / hero shot

### 14.2 禁止退化

- edited frames 基本与 base frames 一样
- 全部只是补光或抛光
- 全部使用同一种镜头变换
- 为了镜头变化改变剧情状态

---

## 15. Story Function to Camera Resolver

编辑阶段先根据剧情功能选镜头，不是只写 `cinematic shot`。

| Story Function | 推荐镜头 | 视觉焦点 |
|---|---|---|
| 开场交代环境 | stronger wide / high-angle wide | 场景结构、主体位置 |
| 觉醒 / 苏醒 | face close-up / eye close-up | 眼睛、面部 |
| 手部动作 / 道具触发 | close-up detail / insert | 手部、道具、触点 |
| 动作爆发 / 攻击 / 轨迹 | side trajectory / diagonal action shot | 方向、速度、冲击 |
| 人物反应 | reaction close-up / OTS | 表情、身体反应 |
| 空间变化 / 阵法范围 | overhead / high-angle | 空间关系、范围 |
| 英雄显现 / 复活完成 | low-angle hero shot | 气势、站姿、标志物 |
| 收束 / 余波 | wide aftermath / symbolic close-up | 结果、余波、情绪落点 |

---

## 16. v6.14 再编辑 Prompt 设计（核心修正版）

这是 v6.14 最关键的部分。

### 16.1 结构原则

再编辑阶段的表层 prompt 必须使用 **双段式结构**：

1. **Story-State / Screen-Content Block**
2. **Camera Transformation Block**

即：

```text
第一段：这帧画面是什么
第二段：这帧怎么拍、强调什么
```

### 16.2 第一段职责：Story-State / Screen-Content Block

负责：
- 继承 base frame 的确切剧情状态；
- 用一句自然语言直接交代这帧画面本身正在呈现什么；
- 可以写人物姿态、动作、道具状态、关键场景状态。

推荐格式：

```text
Use case_base_frame0x.png as the direct source for the exact story state of [character + action + pose + prop state + scene state].
```

允许写入：
- `Nezha fully standing on the glowing lotus after rising from the seated pose`
- `Nezha standing on the glowing lotus in a ready stance with the Universe Ring on his right wrist`
- `Nezha sitting upright on the glowing lotus while the red sash begins to flow outward`
- `Nezha lying on the glowing lotus while the Universe Ring has fitted onto his raised right wrist`

不允许写入：
- `Keep ... present in the frame`
- `focused on ...`
- `close-up`
- `foreground / background`
- `softly visible`
- `close proximity`
- `subtle reflection`

### 16.3 第二段职责：Camera Transformation Block

负责：
- 镜头类型
- 视觉焦点
- 需要强调的画面细节
- 可选的轻量上下文补充

推荐格式：

```text
Camera transformation: create a [shot type] with strong focus on [visual emphasis].
```

或：

```text
Camera transformation: create a [shot type] centered on [visual focus].
```

这里可以写：
- `single-character low-angle wide shot`
- `single-character action close-up`
- `single-character face close-up`
- `single-character seated close-up`
- `close-up detail shot`
- `low-angle hero shot`

可强调：
- `Nezha's full standing pose on the glowing lotus`
- `Nezha's ready stance and the glowing Universe Ring on his right wrist`
- `Nezha's awakened eyes`
- `the raised wrist and the fitted glowing ring`
- `Nezha's upright seated posture and the flowing red sash`

### 16.4 明确禁止项

v6.14 明确取消：
- `Keep ...`
- `Preserve ...`
- `Reframe the same moment without altering ...`
- `Clearly show ...`
- 大量 `Do not ...`
- 资源约束清单
- 未实际输入模型的资源图文件名

### 16.5 v6.14 标准模板

```text
Generate case_edit_frame0x.png: Use case_base_frame0x.png as the direct source for the exact story state of [screen content sentence]. Camera transformation: create a [shot type] with strong focus on [visual emphasis]. [Target style].
```

### 16.6 如果确实需要轻量上下文补充

只允许在镜头块内部自然写一句：

```text
... with the glowing lotus still visible as context.
```

或：

```text
... while the lotus glow remains part of the frame.
```

而不是再写 `Keep ...`。

---

## 17. v6.14 再编辑 Prompt 示例组（哪吒复活）

### 17.1 手腕与乾坤圈

```text
Generate case_edit_frame04.png: Use case_base_frame04.png as the direct source for the exact story state of Nezha lying on the glowing lotus while the Universe Ring has fitted onto his raised right wrist. Camera transformation: create a single-character wrist close-up with strong focus on the raised wrist and the fitted glowing Universe Ring. Cinematic Chinese fantasy 3D animation style.
```

### 17.2 睁眼特写

```text
Generate case_edit_frame05.png: Use case_base_frame05.png as the direct source for the exact story state of Nezha lying on the glowing lotus after the Universe Ring has fitted onto his right wrist. Camera transformation: create a single-character face close-up with strong focus on Nezha's awakened eyes and calm expression. Cinematic Chinese fantasy 3D animation style.
```

### 17.3 坐起与红绫

```text
Generate case_edit_frame06.png: Use case_base_frame06.png as the direct source for the exact story state of Nezha sitting upright on the glowing lotus while the red sash begins to flow outward. Camera transformation: create a single-character seated close-up with strong focus on Nezha's upright seated posture and the flowing red sash. Cinematic Chinese fantasy 3D animation style.
```

### 17.4 站立英雄镜头

```text
Generate case_edit_frame07.png: Use case_base_frame07.png as the direct source for the exact story state of Nezha fully standing on the glowing lotus after rising from the seated pose. Camera transformation: create a single-character low-angle wide shot with strong focus on Nezha's full standing pose on the glowing lotus. Cinematic Chinese fantasy 3D animation style.
```

### 17.5 准备姿态与乾坤圈

```text
Generate case_edit_frame08.png: Use case_base_frame08.png as the direct source for the exact story state of Nezha standing on the glowing lotus in a ready stance with the Universe Ring on his right wrist. Camera transformation: create a single-character action close-up with strong focus on Nezha's ready stance and the glowing Universe Ring on his right wrist. Cinematic Chinese fantasy 3D animation style.
```

### 17.6 收束英雄镜头

```text
Generate case_edit_frame09.png: Use case_base_frame09.png as the direct source for the exact story state of Nezha standing on the glowing lotus after the resurrection is complete. Camera transformation: create a single-character heroic wide shot with strong focus on Nezha's final standing pose on the glowing lotus. Cinematic Chinese fantasy 3D animation style.
```

---

## 18. Edit Prompt Compiler（v6.14）

### 18.1 输入字段

```json
{
  "source_base": "case_base_frame08.png",
  "story_state": "Nezha standing on the glowing lotus in a ready stance with the Universe Ring on his right wrist",
  "camera_type": "single-character action close-up",
  "visual_focus": "Nezha's ready stance and the glowing Universe Ring on his right wrist",
  "style": "Cinematic Chinese fantasy 3D animation style"
}
```

### 18.2 输出规则

```text
Generate [edit_frame]:
Use [base_frame] as the direct source for the exact story state of [story_state].
Camera transformation: create a [camera_type] with strong focus on [visual_focus].
[style]
```

### 18.3 编译原则

- 不输出 Keep / Preserve
- 不输出资源审查项清单
- 不输出长串否定句
- 不输出未实际输入的资源图文件名
- 不把候选评估标准写成表层 prompt

---

## 19. Edit Frame 候选评估优先级

1. Camera Transformation Accuracy
2. Story State Preservation
3. Reference Dependency Preservation
4. Hero Product Consistency
5. Camera Diversity Value
6. Subject / Object Continuity
7. Visible Detail Quality
8. Anchor Preservation
9. Visual Quality

### 19.1 编辑帧硬性否决

- 与 base frame 几乎同景别、同机位、同构图
- 只是补光 / 锐化 / 清背景
- 没有规划中的 camera delta
- 改变了剧情状态
- 改变了人物位置或道具状态
- 人物身份漂移
- 核心商品变形
- 为镜头多样性牺牲叙事连续性

---

## 20. Nine-Grid Fallback Logic

回退逻辑不能直接回退到普通中景。

```text
1. Try the planned special camera.
2. If it fails, keep the same story function and switch to another special camera.
3. If identity or prop consistency fails, retry with additional correction references if necessary.
4. If full-body special shot fails, move closer.
5. If close shot fails, switch angle.
6. Only if all cinematic retries fail, keep the base frame as a temporary placeholder.
```

### 20.1 原则

- 普通中景不是安全回退，而是失败候选
- base frame 只能临时占位，不能直接视为最终 edit frame

---

## 21. 重生成判定

### 21.1 人物资源图需要重生成

- 一张图中有多个同一人物
- 多视角并排
- 多姿势
- 场景背景过多
- 携带了后续不应提前出现的关键道具，但没有 clean 版本

### 21.2 道具资源图需要重生成

- 不是单独展示
- 关键结构不清
- 颜色布局不稳定
- 轮廓不利于后续引用

### 21.3 Base Frame 需要重生成

- 没有正确使用 Reference Resolver 要求的资源图
- 新实体身份错误
- hero product 形态错误
- 使用了不存在的场景锚点
- Scene Core 被遮挡

### 21.4 Edit Frame 需要重生成

- 没有真实 camera delta
- 只是抛光
- 改变剧情状态
- 人物 / 道具状态漂移
- 场景变成另一个地方
- 引入无关人物 / 道具

---

## 22. workflow_summary.json 建议结构

```json
{
  "case_name": "case_name",
  "status": "未完成",
  "grid": 9,
  "idea": "...",
  "resources": {},
  "frame_entity_plan": {},
  "reference_resolver": {},
  "base_frames": {},
  "edit_plan": [],
  "edit_frames": {},
  "final_frames": {}
}
```

### 22.1 edit_plan 推荐字段

```json
{
  "frame": "case_edit_frame08.png",
  "source_base": "case_base_frame08.png",
  "story_state": "Nezha standing on the glowing lotus in a ready stance with the Universe Ring on his right wrist",
  "camera_type": "single-character action close-up",
  "visual_focus": "Nezha's ready stance and the glowing Universe Ring on his right wrist",
  "style": "Cinematic Chinese fantasy 3D animation style"
}
```

---

## 23. 执行口令

### 23.1 扫描资源库后

1. 现有哪些资源文件？
2. 哪些是角色、场景、道具、风格参考、组合资源？
3. 哪些实体是身份敏感的？
4. 哪些道具是 hero product？
5. 是否缺少 clean 版本？
6. 哪些内容绝不能只靠文字临时生成？

### 23.2 生成基础帧前

1. 当前 story beat 是什么？
2. 当前 Scene Core 是什么？
3. 当前帧有哪些实体可见？
4. 哪些实体首次出现？
5. 是否有 hero product 可见？
6. 当前基础镜头是 wide 还是 medium？
7. 哪些参考图由 Reference Resolver 选中？
8. prompt 是否紧凑？

### 23.3 生成编辑帧前（v6.14 重点）

1. 当前 source base frame 是什么？
2. 第一段是否已经完整交代这帧画面内容？
3. 第一段是否避免了 `Keep ...`？
4. 第一段是否没有越界写镜头语义？
5. 第二段是否明确给出镜头类型？
6. 第二段是否明确给出视觉强调点？
7. 是否只提到了实际输入的 base frame？
8. 是否避免把候选评估标准搬进表层 prompt？

### 23.4 候选选择前

1. 是否正确使用了参考图？
2. 新实体是否与资源图一致？
3. hero product 是否稳定？
4. story state 是否正确？
5. Scene Core 是否可见？
6. 对 edit frame 来说，是否存在真实 camera delta？
7. 该候选即使好看，是否也可能因为逻辑错误而应被淘汰？

---

## 24. 一句话总结

```text
v6.14 is a full workflow version in which base frames are generated through dependency-aware resource scheduling, while edit frames are generated with a clean two-block prompt: the first block states the exact screen content of the frame, and the Camera transformation block defines the shot type and the visual emphasis.
```

中文概括：

```text
v6.14 是一版完整工作流：基础帧通过资源依赖调度来锁定剧情与实体一致性；再编辑阶段则通过干净的双段式 prompt 来完成镜头重设计——第一段交代这帧画面内容，第二段描述镜头类型与镜头强调细节。
```
